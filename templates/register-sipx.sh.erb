#!/bin/sh

if [ -e /tmp/<%= clientname %> ]; then 
	exit 0 

else 
	cd /tmp
	wget --user=<%= clientname.downcase %> --password=<%= password %> --no-check-certificate https://<%= mastername %>:8443/sipxconfig/initial-config/<%= clientname.downcase %>

	exit $? 
fi 
